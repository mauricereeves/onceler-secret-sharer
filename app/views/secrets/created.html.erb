<div class="alert alert-success">
  <strong>✅ Secret Created Successfully!</strong>
</div>

<h2>Your Secret URL</h2>

<div class="warning">
  <strong>⚠️ Important:</strong> Save this URL now! Once you leave this page, you won't be able to retrieve it again.
</div>

<div class="form-group">
  <label>Share this URL:</label>
  <div class="url-display">
    <%= @url %>
  </div>
</div>

<div class="form-group">
  <button onclick="copyToClipboard()" class="btn">📋 Copy URL</button>
  <a href="<%= @url %>" class="btn" target="_blank">🔗 Test URL</a>
  <%= link_to "🗑️ Destroy Secret", secret_path(@secret.token), 
              method: :destroy, 
              data: { confirm: "Are you sure? This will permanently destroy the secret and make the URL invalid!" },
              class: "btn",
              style: "background: #dc3545;" %>
</div>

<h3>Details</h3>
<ul>
  <li><strong>Expires:</strong> <%= @secret.expires_at.strftime("%B %d, %Y at %I:%M %p") %></li>
  <li><strong>Max Views:</strong> <%= @secret.max_views %></li>
  <li><strong>Current Views:</strong> <%= @secret.view_count %></li>
</ul>

<p><%= link_to "Create Another Secret", root_path, class: "btn" %></p>

<script>
function copyToClipboard() {
  const url = '<%= @url %>';
  navigator.clipboard.writeText(url).then(function() {
    alert('URL copied to clipboard!');
  });
}
</script>